# vertx3 proxy example

Proxy users request to different backends depending on what group he is assigned. 

Using vertx, guice, mongo. Backend could be located on multiple hosts, mongo could be sharded. 
